module.exports = {
  plugins: {
    // 1. Tailwind CSS must be the first plugin
    'tailwindcss': {},
    // 2. Autoprefixer adds vendor prefixes (like -webkit-, -moz-)
    'autoprefixer': {},
  },
}