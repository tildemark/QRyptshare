# v1.1.0 - Advanced Styling and Quality of Life Update

**Release Date:** November 30, 2025

## ✨ New Features

* **Custom Line Color:** Users can now select any custom color for the QR code lines (foreground).

* **Custom Border/Frame:** Added controls for the QR code frame:

  * Customizable **Border Size** (thickness).

  * Customizable **Border Color**.

  * Customizable **Border Radius** (rounded corners).

* **Quiet Zone Margin:** Added a slider to control the padding (quiet zone) around the QR code matrix.

## 🎨 Styling & UX

* **Fixed Colors:** The QR code background is fixed to **White (`#FFFFFF`)** and the default line color is **Black (`#000000`)** for optimal scannability.

* **Improved Export:** The download function has been updated to correctly capture the custom border, margin, and colors when exporting to PNG, JPG, or WebP.

## 🔧 Architecture

* **Backward Compatible:** This version maintains the stable `react-qr-code` library architecture (Minor update).